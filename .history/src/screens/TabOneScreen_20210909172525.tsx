import { StyleSheet } from 'react-native'

const TabOneScreen = () => {
  return
}

const styles = StyleSheet.create({
  text: {
    fontSize: 30,
    fontWeight: 'bold',
    color: '#56078d',
  },
})

export default TabOneScreen
